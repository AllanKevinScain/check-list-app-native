import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  container: {
    borderRadius: 40,
    borderWidth: 1,
    padding: 2,
  },
  ball: {
    width: "100%",
    height: "100%",
  },
});
